# Bluwhale image edits

These images are rebranded design references, not screenshots of the current running application. They were edited with the built-in `image_gen` tool. The website uses embedded HTML/CSS and the code-native `ui/static/images/bluwhale-wordmark.svg` for its responsive landing wordmark.

## Landing page

Output: [`landingpage.png`](landingpage.png)

Final prompt:

> Use case: text-localization. Edit target: the provided landing-page screenshot. Replace the top-left navigation wordmark 'Zaraba' with exactly 'Bluwhale'. Replace the large centered blue dot-matrix wordmark 'ZARABA' with exactly 'BLUWHALE' (B L U W H A L E), retaining the same electric-blue dot-matrix texture, lettering height and centered placement; widen the lettering area as needed to fit the longer name. Preserve the existing dark background, subtle grid, original small white navigation symbol, all other navigation labels, headline, body copy, buttons and wave artwork. This is a precise rebranding edit, not a redesign. Preserve the complete screenshot framing and original wide aspect ratio. Do not add anything, crop, or create a collage. Output one edited image.

## Other screens

Outputs: [`markets.png`](markets.png), [`trade.png`](trade.png), [`wallet.png`](wallet.png)

The following final prompt was applied separately to each original screenshot:

> Use case: text-localization. This screenshot is the edit target. Replace only the top-left navigation brand text 'Zaraba' with exactly 'Bluwhale' (B l u w h a l e), matching the original white font, weight and baseline. Allow the brand text a little extra horizontal width while keeping a clean gap before Markets. Preserve the existing small white logo, all other navigation labels and icons, all page content, numbers, charts, controls, colors, spacing, screenshot framing and wide aspect ratio. This is a precise text-only branding edit. Do not redesign, crop, or add any elements. Return one full edited screenshot.

