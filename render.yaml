services:
  - type: web
    name: blu-exchange
    runtime: go
    plan: free
    healthCheckPath: /ping
    buildCommand: go build -trimpath -ldflags="-s -w" -o bin/exchange ./cmd/exchange
    startCommand: ./bin/exchange
    envVars:
      - key: GO_VERSION
        value: "1.27.1"
